using UnityEngine;
using System.Collections;

public class StatCalculations
{
	/*private float enduranceModifier = 0.15f;
	private float smartsModifier = 0.15f;
	private float powerModifier = 0.1f;
	private float speedModifier = 0.15f;

	public enum StatType
	{
		ENDURANCE,
		SMARTS,
		POWER,
		SPEED
	}

	public int CalculateStat(int statVal, StatType stateType)
	{
		float modifier;
		if(StatType == StatType.ENDURANCE)
		{
			modifier = enduranceModifier;
			return (statVal + (int)(statVal * modifier));
		}
		if(StatType == StatType.SMARTS)
		{
			modifier = smartsModifier;
			return (statVal + (int)(statVal * modifier));
		}
		if(StatType == StatType.POWER)
		{
			modifier = powerModifier;
			return (statVal + (int)(statVal * modifier));
		}
		if(StatType == StatType.SPEED)
		{
			modifier = speedModifier;
			return (statVal + (int)(statVal * modifier));
		}
		return 0;
	}*/
}
